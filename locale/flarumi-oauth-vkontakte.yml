fof-oauth:
  admin:
    settings:
      providers:
        vkontakte:
          client_id_label: => fof-oauth.ref.settings.client_id
          client_secret_label: => fof-oauth.ref.settings.client_secret
          description: Tətbiqi burada qeydiyyatdan keçirə bilərsiniz {link}
  forum:
    log_in:
      with_button: '{provayder} ilə daxil olun'
      with_vkontakte: '{provayder} ilə daxil olun'
      with_vkontakte_button: => fof-oauth.forum.log_in.with_button
    providers:
      vkontakte: => fof-oauth.lib.providers.vkontakte
  lib:
    providers:
      vkontakte: VK
  providers:
    vkontakte: => fof-oauth.lib.providers.vkontakte
