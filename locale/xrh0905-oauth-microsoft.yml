fof-oauth:
  admin:
    settings:
      providers:
        microsoft:
          client_id_label: => fof-oauth.ref.settings.client_id
          client_secret_label: => fof-oauth.ref.settings.client_secret
          description: Forumunuzu Microsoft ilə qeydiyyatdan keçirin {link}
  forum:
    log_in:
      with_microsoft_button: => fof-oauth.forum.log_in.with_button
    providers:
      microsoft: => fof-oauth.lib.providers.microsoft
  lib:
    providers:
      microsoft: Microsoft
