fof-oauth:
  admin:
    settings:
      providers:
        slack:
          client_id_label: => fof-oauth.ref.settings.client_id
          client_secret_label: => fof-oauth.ref.settings.client_secret
          description: Slack {link} ilə forumunuzu yadda saxlayın
  forum:
    log_in:
      with_slack_button: Slack ilə daxil olun
    providers:
      slack: => fof-oauth.lib.providers.slack
  lib:
    providers:
      slack: Sülh
